# frontend

**Owner:** _assign a teammate — decide as a group_

Choose one of: Thymeleaf, Angular, Vue, or React (must be justified in
this README once chosen, per the case study's mandatory tech stack rule).

Needs to talk to the system only through the `api-gateway` on port 8080
— never directly to individual hazard services.

## Checklist
- [ ] Pick and justify the framework here
- [ ] Login screen (talks to auth-service via gateway)
- [ ] Ward recorder forms (one per hazard)
- [ ] Supervisor approval queue
- [ ] Dashboard with counts + interactive map (approved incidents only)
- [ ] Report download UI (PDF/DOCX/XLSX/CSV)
