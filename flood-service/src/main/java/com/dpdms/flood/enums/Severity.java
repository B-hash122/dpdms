package com.dpdms.flood.enums;

/**
 * Part of the shared incident metadata model used consistently across
 * all five hazard services so the dashboard and reports can aggregate
 * uniformly (per the case study's "shared incident metadata" requirement).
 */
public enum Severity {
    LOW,
    MODERATE,
    HIGH,
    CRITICAL
}
